/**
 * @deprecated Use `useSimulationState` from `../state/SimulationStateContext` instead.
 * Kept for imports that expect the old hook name.
 */
export { useSimulationState as useSimulationWebSocket } from '../state/SimulationStateContext'
